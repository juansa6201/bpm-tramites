/** Circuito del trámite. Espejo del enum de Prisma, pero independiente. */
export enum OrigenTramite {
  INTERNO_INTERNO = 'INTERNO_INTERNO',
  INTERNO_EXTERNO = 'INTERNO_EXTERNO',
  EXTERNO_INTERNO = 'EXTERNO_INTERNO',
}
